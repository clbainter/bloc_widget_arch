name: example
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  bloc_widget_arch:
    path: ../../bloc_widget_arch
  cupertino_icons: ^1.0.2
  event_bus: ^2.0.0
  flutter_modular: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true